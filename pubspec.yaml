name: task_manager
description: TaskFlow Flutter task manager.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.4.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.5.0
  firebase_core: ^4.8.0
  google_sign_in: ^6.2.1
  http: ^1.6.0
  intl: ^0.20.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
